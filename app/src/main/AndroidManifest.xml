<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.joe.cst2335finalgroupproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/m_app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".m_MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".a_ActivityTrackerActivity" />
        <activity android:name=".t_ThermostatProgramActivity" />
        <activity android:name=".c_CarTrackerActivity" android:configChanges="keyboardHidden|orientation" />
        <activity android:name=".a_DetailView" />
        <activity android:name=".t_DetailView" />
        <activity android:name=".c_AddFuelDetailsActivity" />
        <activity android:name=".c_EditFuelDetailsActivity" />
        <activity android:name=".n_NutritionTrackerActivity" />
        <activity android:name=".c_FuelStatisticsActivity"></activity>
    </application>

</manifest>